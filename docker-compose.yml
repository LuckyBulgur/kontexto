services:
  kontexto:
    build: .
    ports:
      - "8080:80"
    volumes:
      - kontexto-data:/app/data
    environment:
      - KONTEXTO_DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  kontexto-data:
